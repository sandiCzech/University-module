{block content}

	<div class="page-heading">	
		<h1>{_'Add/edit absolvent'}</h1>
	</div>

	<div class="control-group">
		<a n:href="default idPage => $idPage" class="back">{_'Back to absolvents'}</a>
	</div>

	{form form}

		<div class="control-group row">

			<div class="col-md-10">

				<div class="form-group">
					{label name /}
					{input name class => 'form-control'}
		        </div>

		        <div class="form-group">
					{label department /}
					{input department class => 'form-control'}
		        </div>

		        <div class="form-group">
					{label perex /}
					{input perex class => 'form-control editor'}
		        </div>

		        <div class="form-group">
					{label text /}
					{input text class => 'form-control editor'}
		        </div>

		        <a n:href=":Admin:Filesystem:default dialog => true, multiple => false" data-container="#pictures" class="btn btn-primary ajax filesDialog" data-remote="false" data-target="#myModal" data-toggle="modal">{_'Load photo'}</a>

	        </div>

	        <div class="col-md-10" id="pictures">
	        	{if $absolvent}
	        		{if $absolvent->getPhoto()}
						<div class="col-md-3 jq_fileBox">
							<div class="img-thumbnail">
								<img src="{$basePath}{$absolvent->getPhoto()->getPath()|thumbnail:'absolvent'}" />
								<input type="hidden" name="files[]" value="{$absolvent->getPhoto()->getPath()}" />
								<input class="form-control" type="text" name="fileNames[]" value="{$absolvent->getPhoto()->getTitle()}" />
								<span class="btn btn-default jq_fileDelete">&times</span>
							</div>
						</div>
					{/if}
				{/if}
	        </div>



        </div>

        <div class="form-actions">

			{input save class => 'btn btn-success'}

		</div>


	{/form}

{/block}
